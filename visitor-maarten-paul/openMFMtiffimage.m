function openMFMtiffimage(pathname,filesave,h,w,planes,channel)
    
    img= bfopen(fullfile(pathname,filesave));

    stack = zeros(size([h,w,planes,channel,frames]))
    for (i 1:)


return stack
end